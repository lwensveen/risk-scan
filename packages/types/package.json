{
  "name": "@risk-scan/types",
  "description": "Shared Zod schemas & TypeScript types for RiskScan monorepo",
  "version": "1.0.0",
  "dependencies": {
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@risk-scan/eslint-config": "workspace:*",
    "@risk-scan/typescript-config": "workspace:*",
    "@types/node": "^24.0.15",
    "eslint": "^9.31.0"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "type": "module",
  "types": "dist/index.d.ts"
}
